const translator = require("../translate");

describe("When translating a payload", () => {
  let result;  

  beforeAll(() => {
    let event = {};
  });
  
  test("something", async () => {
    expect(result).toBe(undefined);
  });
});
