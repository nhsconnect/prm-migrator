version: 0.2

phases:
  pre_build:
    commands:
    - chmod 755 ./packages/modules/opentest/bouncerw
  build:
    commands:
    - cd ./env/${ENVIRONMENT}-${ACCOUNT_ID} 
    - terragrunt apply-all --terragrunt-non-interactive -no-color
